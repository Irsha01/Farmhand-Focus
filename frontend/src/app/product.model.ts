export interface Product {
    id: number;
    name: string;
    price: number;
    quantity: number;
    image?: string;
    description: string; // Add the 'description' property,
    productId:number
  }